---
import Seo from '../components/Seo.astro';
import { withBasePath } from '../lib/url';
import { NAVIGATION, SITE } from '../site.config';
import '../styles/global.css';

type Props = {
  title?: string;
  description?: string;
  canonical?: string;
  image?: string;
  jsonLd?: Record<string, unknown> | Record<string, unknown>[];
};

const { title, description, canonical, image, jsonLd } = Astro.props;
---

<!doctype html>
<html lang="de">
  <head>
    <Seo
      title={title}
      description={description}
      canonical={canonical}
      image={image}
      jsonLd={jsonLd}
    />
  </head>
  <body>
    <header class="site-header">
      <div class="container header-inner">
        <a class="brand" href={withBasePath('/')} aria-label={`${SITE.name} home`}>
          <img src={withBasePath('/images/orderlyze/logo.png')} alt="Orderlyze" width="205" height="52" />
        </a>
        <nav aria-label="Primary navigation">
          {
            NAVIGATION.map((item) => (
              <a href={withBasePath(item.href)}>{item.label}</a>
            ))
          }
        </nav>
        <a class="button secondary header-cta" href={withBasePath('/angebot/')}>Angebot</a>
      </div>
    </header>
    <main>
      <slot />
    </main>
    <footer class="site-footer">
      <div class="container footer-inner">
        <p>{SITE.name} Kassensystem. Einfach zu bedienen, schnell eingerichtet und finanzamtkonform.</p>
        <div class="footer-links">
          <a href={withBasePath('/impressum/')}>Impressum</a>
          <a href={withBasePath('/datenschutzerklaerung/')}>Datenschutz</a>
          <a href={withBasePath('/agb/')}>AGB</a>
        </div>
      </div>
    </footer>
  </body>
</html>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 20;
    border-bottom: 1px solid rgb(217 222 231 / 78%);
    background: rgb(251 252 254 / 88%);
    backdrop-filter: blur(16px);
  }

  .header-inner {
    display: flex;
    min-height: 72px;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
  }

  .brand {
    display: inline-flex;
    align-items: center;
  }

  .brand img {
    width: 146px;
    height: auto;
  }

  nav {
    display: flex;
    align-items: center;
    gap: 22px;
    color: #374151;
    font-size: 0.94rem;
    font-weight: 750;
  }

  .site-footer {
    border-top: 1px solid var(--line);
    background: white;
  }

  .footer-inner {
    display: flex;
    min-height: 96px;
    align-items: center;
    justify-content: space-between;
    gap: 20px;
    color: var(--muted);
  }

  .footer-inner a {
    font-weight: 800;
    color: var(--ink);
  }

  .footer-links {
    display: flex;
    flex-wrap: wrap;
    gap: 16px;
  }

  @media (max-width: 840px) {
    nav {
      display: none;
    }
  }

  @media (max-width: 520px) {
    .header-cta {
      display: none;
    }

    .footer-inner {
      align-items: flex-start;
      flex-direction: column;
      justify-content: center;
    }
  }
</style>
